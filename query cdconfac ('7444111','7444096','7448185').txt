<?xml version="1.0" encoding="UTF-8"?>
<Queries>
	<QueryInfo>
		<Pool>informixPool</Pool>
		<Tipo>Select</Tipo>
		<Query><![CDATA[
		SELECT 'TRSFCOBFAC',wc.*,'TSFFACTURC',f.*,'TRSFCOMFACT',co.*,'TRSFINTFACT',ifct.*,'TSFCOBROSCONF',c.*,'TSFEMPRESA',e.*,'TSFOPECONF',op.*,'TSFOPERACI',o.*,'tsfcontrato',con.*FROM TRSFCOBFAC wc  JOIN TSFFACTURC f    ON f.CDEMPRES = wc.CDEMPRES   AND f.CDOPERAC = wc.CDOPERAC   AND f.CDREMESA = wc.CDREMESA   AND f.NUREMSDF = wc.NUREMSDF   AND f.CDPROVEE = wc.CDPROVEE   AND f.NUANIO = wc.NUANIO   AND f.CDFACTURA = wc.CDFACTURA  JOIN TRSFCOMFACT co    ON wc.CDEMPRES = co.CDEMPRES   AND wc.CDOPERAC = co.CDOPERAC   AND wc.CDREMESA = co.CDREMESA   AND wc.NUREMSDF = co.NUREMSDF   AND wc.CDPROVEE = co.CDPROVEE   AND wc.NUANIO = co.NUANIO   AND wc.CDFACTURA = co.CDFACTURA   AND co.CDCOMISI = 'C15'  JOIN TRSFINTFACT ifct    ON f.CDEMPRES = ifct.CDEMPRES   AND f.CDOPERAC = ifct.CDOPERAC   AND f.NUREMSDF = ifct.NUREMSDF   AND f.CDPROVEE = ifct.CDPROVEE   AND f.NUANIO = ifct.NUANIO   AND f.CDFACTURA = ifct.CDFACTURA  JOIN TSFCOBROSCONF c    ON c.CDEMPRES = wc.CDEMPRES   AND c.CDOPERAC = wc.CDOPERAC   AND c.CDCOBRO = wc.CDCOBRO   AND c.cdconcep IN ('C14', 'C19')   JOIN TSFEMPRESA e ON e.CDEMPRES = wc.CDEMPRES  JOIN TSFOPECONF op    ON op.CDEMPRES = wc.CDEMPRES   AND op.CDOPERAC = wc.CDOPERAC  JOIN TSFOPERACI o    ON o.CDEMPRES = wc.CDEMPRES   AND o.CDOPERAC = wc.CDOPERAC  JOIN tsfcontrato con    ON con.cdempres = F.cdempres   AND con.cdoperac = F.cdoperacWHERE f.cdconfac in ('7444111','7444096','7448185')
		]]></Query>
	</QueryInfo>
</Queries>